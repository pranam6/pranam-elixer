<h2>Shorten a URL</h2>
<%= form_for @changeset, Routes.url_path(@conn, :create), fn f -> %>
  <div>
    <%= label f, :long_url %>
    <%= text_input f, :long_url %>
    <%= error_tag f, :long_url %>
  </div>
  <div>
    <%= submit "Shorten" %>
  </div>
<% end %>
